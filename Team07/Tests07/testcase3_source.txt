procedure main {
    print main; 
    read procedure; 
    procedure = procedure + 1; 

    if (procedure == w - x * y + z) then { 
        print main; 
        read procedure; 
        procedure = procedure + 1; 

        if (procedure >= w - x * y + z) then { 
            while (x >= y) { 
                read w; 
                x = x - w * 2 - z; 
                print x; 
            }
        } else {
            read z;
            procedure = procedure + z; 
        }
    } else {
        main = 2; 
    }

    read x; 
    read y; 
    print y; 

    z = (x * 2) + y / y; 
    z = z / 42; 

    t = (g + o + o + n) / 0; 
    t = z * 10000000000; 
    z = (z + x / 4 + y / 5) % main; 
}